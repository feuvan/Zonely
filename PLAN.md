# Zonely 计划

> 一个通过鼠标拖动快速定位和调整 macOS 窗口大小的桌面工具。

## 当前状态

- 阶段：M2 原型开发
- 当前目标：验证真实窗口拖动、区域预览和自动 resize 的完整闭环
- 记录方式：完成一项任务后勾选复选框，并在必要时补充决策、日期或备注

## 产品目标

让用户通过一次自然的鼠标拖动完成窗口布局：拖动窗口时显示可用区域，松开鼠标后将窗口移动并调整到目标区域。

核心体验：

1. 用户按住窗口标题栏开始拖动。
2. Zonely 识别拖动位置，并显示候选区域预览。
3. 用户将窗口拖到目标区域后松开鼠标。
4. 窗口自动吸附到该区域并完成移动和 resize。
5. 全流程尽量不打断用户当前工作，也不要求每次打开主界面。

## 范围

### MVP 包含

- [x] macOS 菜单栏应用
- [x] Accessibility 权限检测、引导和状态展示
- [x] 全局鼠标拖动监听
- [~] 识别窗口拖动开始、移动和结束（当前使用标题栏区域启发式判断）
- [x] 单屏区域布局：左半屏、右半屏、全屏、四角区域
- [x] 拖动过程中的区域预览
- [x] 使用 Accessibility API 移动和调整窗口大小
- [x] 多显示器和不同屏幕缩放比例的基本支持
- [x] 菜单栏中的启用/暂停、设置入口和退出操作
- [x] 无权限、不可调整大小窗口、全屏窗口等情况的友好处理

### MVP 暂不包含

- 自定义复杂布局编辑器
- 云同步和账号系统
- 团队协作
- 窗口布局持久化恢复
- 自动启动以外的系统级增强功能
- Windows 或 Linux 版本

## 里程碑

### M0：项目基础

- [x] 创建 GitHub 仓库
- [x] 创建基础 README 和 `.gitignore`
- [x] 创建本计划文件
- [x] 确定最低支持的 macOS 版本（macOS 13）
- [x] 确定 Swift 和 Xcode 版本（SwiftPM 5.10，当前使用 Xcode 26.6）
- [x] 创建 AppKit 菜单栏应用工程
- [x] 建立 Debug 和 Release 构建配置
- [x] 添加基础单元测试目标

### M1：窗口控制原型

- [x] 实现 Accessibility 权限检查
- [x] 读取当前前台应用和窗口信息
- [x] 读取窗口位置、大小和可调整状态
- [x] 实现窗口位置和大小更新
- [x] 建立一个可以手动触发的测试布局命令
- [ ] 验证 Finder、Safari、终端和常见 Electron 应用
- [ ] 记录 Accessibility API 在不同应用上的兼容性问题

### M2：鼠标拖动和区域吸附

- [x] 选择并实现全局鼠标事件监听方案
- [~] 识别窗口拖动手势的开始和结束
- [~] 过滤点击、文本选择、滚动和其他非窗口拖动事件（当前按标题栏区域过滤）
- [x] 计算当前鼠标所在屏幕和可用工作区域
- [x] 实现半屏、全屏和四角区域模型
- [x] 实现区域命中判断和边缘容差
- [x] 实现目标区域预览层
- [x] 松开鼠标后执行窗口移动和 resize
- [x] 增加拖动取消和异常恢复逻辑

### M3：多屏幕和交互体验

- [x] 支持多显示器切换（基础版本）
- [x] 处理刘海、菜单栏、Dock 和安全区域
- [x] 处理不同显示器缩放比例（依赖 NSScreen.visibleFrame）
- [ ] 处理显示器连接、断开和分辨率变化
- [ ] 允许用户调整吸附触发距离和预览延迟
- [ ] 允许用户关闭指定区域
- [x] 支持启用/暂停 Zonely
- [x] 增加可访问的菜单栏状态和错误提示
- [x] 确保预览层不会抢走窗口焦点

### M4：稳定性和可发布性

- [~] 补充区域计算和状态机单元测试
- [ ] 建立常见应用兼容性测试清单
- [ ] 测试权限拒绝、权限撤销和应用重启场景
- [ ] 测试全屏、最小化、最大化和不可调整大小窗口
- [ ] 测试高频拖动、快速切屏和显示器热插拔
- [ ] 降低事件监听和预览渲染的 CPU 占用
- [ ] 增加日志开关和错误诊断信息
- [ ] 完成应用图标、名称和菜单文案
- [x] 建立 GitLab CI：运行测试、构建 macOS `.app`、生成 ZIP 和 tag Release
- [ ] 配置代码签名和 notarization
- [ ] 生成可安装的 DMG 或其他发布包
- [ ] 编写安装、权限设置和卸载说明
- [ ] 发布第一个可用版本

## 技术方向

- **语言和 UI**：Swift + AppKit
- **应用形态**：菜单栏应用，默认不显示 Dock 图标
- **窗口控制**：Accessibility API（`AXUIElement`）
- **输入监听**：优先采用系统级鼠标事件监听，并限制监听范围和生命周期
- **区域预览**：独立的无焦点透明窗口，跟随当前屏幕工作区域绘制
- **布局模型**：将屏幕可用区域抽象为可组合的标准区域，窗口执行层只接收最终 frame
- **权限策略**：首次启动清晰说明用途，提供跳转系统设置的入口，并实时反映授权状态

## 需要尽早确定的决策

- [x] 最低支持 macOS 版本
- [ ] 是否只支持 Intel 和 Apple Silicon，还是同时提供 universal binary
- [x] 默认拖动触发区域和触发延迟（当前为标题栏 80pt、边缘 25%）
- [x] MVP 是否包含键盘快捷键（首版不包含）
- [x] MVP 是否包含用户自定义布局（首版不包含）
- [ ] 应用采用开源许可证还是暂不公开源代码
- [ ] 首个版本的发布渠道：GitHub Releases、Homebrew 或两者

## 风险与应对

| 风险 | 影响 | 应对方式 |
| --- | --- | --- |
| Accessibility 权限未开启 | 无法读取或控制窗口 | 启动时检查权限，提供明确引导和重新检查按钮 |
| 不同应用对 Accessibility API 支持不一致 | 部分窗口无法移动或 resize | 在执行前检查窗口属性，失败时保留原位置并提示 |
| 全局事件监听影响系统体验 | 误触或性能问题 | 只在满足拖动条件时处理事件，及时释放监听资源 |
| 多显示器坐标系和缩放比例复杂 | 窗口落点偏移 | 统一使用屏幕坐标转换，并覆盖真实设备测试 |
| 预览层遮挡或抢焦点 | 拖动体验中断 | 使用无焦点窗口和明确的窗口层级配置 |
| 系统升级导致行为变化 | 兼容性回归 | 固定最低版本，保留兼容性测试矩阵和诊断日志 |

## 验收标准

### MVP 验收

- [ ] 开启权限后，用户可以从常见应用拖动窗口到半屏、全屏和四角区域
- [ ] 预览区域在拖动过程中稳定显示，松开鼠标后窗口准确落位
- [ ] 目标区域位于另一块显示器时，窗口仍能正确移动和调整大小
- [ ] 暂停 Zonely 后不会继续拦截或处理窗口拖动
- [ ] 没有权限或窗口不支持操作时，应用不会崩溃，并能给出可理解的提示
- [ ] 退出并重新启动应用后，基础功能仍可用

## 开发记录

| 日期 | 记录 |
| --- | --- |
| 2026-09-08 | 初始化 Zonely 仓库和项目计划。 |
| 2026-09-08 | 完成 AppKit 菜单栏应用、Accessibility 窗口控制、标准布局、拖动监听和预览原型；`swift test` 与 `.app` 构建通过。 |
| 2026-09-08 | 增加 GitLab CI/CD：macOS 测试、ARM64 打包、SHA-256 校验和 tag Release。 |
